@model CleanArchMVC.Application.DTO.CategoryDTO
@{
    ViewData["Title"] = "Category Details";
}

<h2>@ViewData["Title"]</h2>
<hr>
<div>
    <hr>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" title="Update" class="btn btn-warning">Edit</a>
    <a asp-action="Delete" asp-route-id="@Model.Id" title="Delete" class="btn btn-danger">Delete</a>
    <a class="btn btn-info" asp-action="Index">Back to List</a>
</div>